export const INCREMENT="INCREMNENT"
export const DECREMENT="DECREMENT"

export const incrementCounter=(payload)=>({
    type:INCREMENT,
    payload
})

export const decrementCounter=(payload)=>({
    type:INCREMENT,
    payload
})